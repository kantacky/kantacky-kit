# ``Camera``

A library for capturing video frames from the device camera as `CGImage` streams.

## Overview

The Camera library provides a simple interface to capture video frames using `AVCaptureSession`. It delivers frames as an `AsyncStream<CGImage>`, making it easy to integrate with Swift concurrency.

## Topics

### Capturing Video Frames

- ``CameraManager``
- ``CameraError``
